# Railway / Docker runtime variables
PORT=8080
TZ=Europe/Moscow

# Required for stable secure sessions. Set the real value in Railway Variables only.
SESSION_SECRET_KEY=replace_with_long_random_secret

# Store generated admin config in the persistent rawdata directory.
SSA_DATA_DIR=rawdata

# Leave disabled on Railway. Railway provides HTTPS at the edge.
USE_HTTPS=0
